import data.ListNode
import data.ZooKeeper
import java.lang.Math.pow
import java.math.BigInteger
import java.util.*
import kotlin.Comparator
import kotlin.math.*

class Main {
    class DoubleLinkedList(var `val`: Int) {
        var pre: DoubleLinkedList? = null
        var next: DoubleLinkedList? = null
    }

    companion object {
        class Encrypter(keys: CharArray, values: Array<String>, dictionary: Array<String>) {

            private val keys = keys
            private val values = values
            private val dictionary = dictionary

            fun encrypt(word1: String): String {

            }

            fun decrypt(word2: String): Int {

            }

        }
        @JvmStatic
        fun main(args: Array<String>) {

        }


    }
}